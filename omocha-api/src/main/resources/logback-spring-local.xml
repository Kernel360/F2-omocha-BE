<included>

    <!-- LOG_DIR과 LOG_PATTERN을 직접 정의 -->
    <property name="LOG_DIR" value="./data/logs"/>
    <property name="DATE_FORMAT" value="%d{yyyy-MM-dd}"/>

    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss}){white} %clr(%-5level){highlight}[%clr(%thread){magenta}] [%clr(%logger{0}){cyan}:%clr(%line){blue}] - %msg%n"/>
    <property name="FILE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss} %-5level[%thread] [%logger{0}:%line] - %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="REQUEST" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/request.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/archive/request.${DATE_FORMAT}_%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize> <!--로그 파일의 최대 크기-->
            <maxHistory>60</maxHistory> <!--로그 파일 최대 보관주기 (단위: 일)-->
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <outputPatternAsHeader>true</outputPatternAsHeader> <!--File Header Form-->
        </encoder>
    </appender>

    <!-- 경고 로그 앱렌더 -->
    <appender name="WARN" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/warn.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>WARN</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/archive/warn.${DATE_FORMAT}_%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize> <!-- 로그 파일의 최대 크기 -->
            <maxHistory>60</maxHistory> <!-- 로그 파일 최대 보관주기 (단위: 일) -->
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <outputPatternAsHeader>true</outputPatternAsHeader> <!-- 파일 헤더 형식 -->
        </encoder>
    </appender>

    <!-- 에러 로그 앱렌더 -->
    <appender name="ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/error.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/archive/error.${DATE_FORMAT}_%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize> <!-- 로그 파일의 최대 크기 -->
            <maxHistory>60</maxHistory> <!-- 로그 파일 최대 보관주기 (단위: 일) -->
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <outputPatternAsHeader>true</outputPatternAsHeader> <!-- 파일 헤더 형식 -->
        </encoder>
    </appender>

    <logger name="org.omocha" level="INFO"/>

    <!-- 루트 로거 설정 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="REQUEST"/>
        <appender-ref ref="WARN"/>
        <appender-ref ref="ERROR"/>
    </root>

</included>